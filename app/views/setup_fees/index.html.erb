
      <div class="content-wrapper">
  <div class="row">

    <div class="col-lg-12 d-flex grid-margin stretch-card">
                <div class="card">
                  <div class="card-body">
                    <div class="d-flex flex-wrap justify-content-between">
                      <h4 class="card-title mb-3"> Fees</h4>
   
                    </div>

                    <div class="table-responsive">
                      <table class="table">
  <thead>
    <tr >
      <td scope="col">Name</td>
       <td scope="col">Class</td>
      <td scope="col">Amount</td>
    

    </tr>
  </thead>

  <tbody>
  <% @setup_fees.each do |setup_fee| %>
      <tr scope="row">
      <td><%= setup_fee.name %></td>
       <td><%= setup_fee.class_id %></td>
       <td>GHc<%= setup_fee.amount %></td>
            
       <td><%= link_to 'Edit', edit_setup_fee_path(setup_fee),class: 'btn btn-success' %></td> 
       <td><%= link_to 'Destroy', setup_fee, method: :delete, class: 'btn btn-primary',data: { confirm: 'Are you sure?' } %></td>
  
      </tr>
    <% end %>
  </tbody>
</table>

  
<br>



      </div>
      <%= link_to 'New Setup Fee', new_setup_fee_path,class: 'btn btn-info' %>
                  </div>
                </div>
              </div>
              
            </div>
            
          </div>
